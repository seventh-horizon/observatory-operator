# Go / Python / OS cruft
bin/
dist/
build/
__pycache__/
*.pyc
*.log
.DS_Store
# Obsidian cache if this ever becomes a vault
.obsidian/workspace.json
.trash/
# Archives (we already extracted contents)
*.zip
.venv/

# allow release artifacts in dist/
!dist/*.zip
!dist/*.sha256
*.zip
out/
