SHELL := /bin/bash
.ONESHELL:
.SHELLFLAGS := -eu -o pipefail -c
MAKEFLAGS += --no-builtin-rules

.PHONY: prepush bundle

prepush:
	@echo "[prepush] run lint/tests here (scaffold)"
	@python -V

bundle:
	@echo "[bundle] scaffold — replace with scripts/release-bundle.sh"
